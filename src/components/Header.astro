---
import HeaderTags from "./HeaderTags.astro";

export interface Props {
  heading: string;
}

const { heading } = Astro.props;
---

<header class="flex flex-col gap-4">
  <HeaderTags class="self-end" />
  <h1>{heading}</h1>
</header>
